import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  devIndicators: false,
  // allowedDevOrigins: ['192.168.0.10', 'localhost:3000', 'localhost:8080', '127.0.0.1', '0.0.0.0'],
  /* config options here */
};

export default nextConfig;
